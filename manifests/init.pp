class zenoss {
	
	# Not sure about the core installation because there are several different methods.
	# Source checkout with deps is the only standardised cross platform install
	# With the exception of dev.zenoss.org debian repo
	
	
}